# NBME Clinical Notes Analysis

Clinical concept extraction and summarization from NBME patient notes.

## Setup
1. Install requirements: `pip install -r requirements.txt`
2. Run notebooks in order: exploration → modeling → results
